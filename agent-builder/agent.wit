package component:agent;

interface host-capabilities {
  ask: func(prompt: string) -> string;
}

interface host-console {
  log: func(msg: string);
}

world agent {
  import host-capabilities;
  import host-console;
  export run-code: func(code: string) -> string;
}